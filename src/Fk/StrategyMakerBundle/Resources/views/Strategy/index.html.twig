{% extends "FkStrategyMakerBundle::layout.html.twig" %}
{% block content %}
<div style="text-align: right">
  <a class="btn btn-primary btn-large" href="{{ path('strategy_new') }}"><i class="icon-plus icon-white"></i>Strategy</a>
</div>
{% for entity in entities %}
<div class="dark-block">
  <h3>
    <a href="{{ path('strategy_show', { 'id': entity.id }) }}">{{ entity.title }}</a>
  </h3>
  <p>
    {{ entity.vision }}
  </p>
</div>
{% endfor %}
<!--
<h1>Strategy list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Vision</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('strategy_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.vision }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('strategy_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('strategy_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('strategy_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
-->
{% endblock %}